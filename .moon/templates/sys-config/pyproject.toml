[project]
name = "{{ project_name }}"
version = "0.1.0"
description = "RAG Facile workspace"
requires-python = ">=3.13"
dependencies = []

[dependency-groups]
dev = ["ruff>=0.9", "ty>=0.0.1a7"]

[tool.uv]
managed = true

[tool.uv.workspace]
members = ["apps/*", "packages/*"]
