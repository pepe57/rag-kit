[project]
name = "memory"
version = "0.18.2" # x-release-please-version
description = "Flat-file persistent memory — Markdown stores, grep-based retrieval"
readme = "README.md"
requires-python = ">=3.13"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/rag_facile"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
